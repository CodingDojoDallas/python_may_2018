* Registration
  - Form is present and HTML validations used (via required attribute). No backend
    validations and registration doesn't function, only redirects.
* Login:
  - Form is present and HTML validations used (via required attribute). No backend
    validations and login doesn't function, only redirects.
* Logout
 - Not present.
* Appointment tables:
  - Current day table is set up but not populated with data.
  - Other appointments table is set up but not populated with data.
* Appointment edit:
  - Not present.
* Appointment delete:
  - No functionality.
* Appointment add:
  - Broken redirect. Only frontend validation.

Code:
* Looks like a lot of time was spent on the frontend. While the exam looks really nice,
  the functionality isn't there.
* Views file nearly empty?
